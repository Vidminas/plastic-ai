# Please consult our docs for more info: https://www.librechat.ai/docs/configuration/docker_override

# TO USE THIS FILE, FIRST UNCOMMENT THE LINE ('services:')

# THEN UNCOMMENT ONLY THE SECTION OR SECTIONS CONTAINING THE CHANGES YOU WANT TO APPLY
# SAVE THIS FILE AS 'docker-compose.override.yaml'
# AND USE THE 'docker compose build' & 'docker compose up -d' COMMANDS AS YOU WOULD NORMALLY DO

# WARNING: YOU CAN ONLY SPECIFY EVERY SERVICE NAME ONCE (api, mongodb, meilisearch, ...)
# IF YOU WANT TO OVERRIDE MULTIPLE SETTINGS IN ONE SERVICE YOU WILL HAVE TO EDIT ACCORDINGLY

# EXAMPLE: if you want to use the config file and the latest numbered release docker image the result will be:

services:
  api:
    image: librechat
    build:
      context: .
      target: node
    volumes:
    - type: bind
      source: ${PWD}/librechat.yaml
      target: /app/librechat.yaml
    # Legacy Mongo-backed dependency configuration retained for rollback reference:
    # depends_on:
    #   mongodb:
    #     condition: service_started
    depends_on: !override
      # rag_api:
      #   condition: service_started
      ministack:
        condition: service_healthy
      dynamodb-init:
        condition: service_completed_successfully
      s3-init:
        condition: service_completed_successfully
      bedrock-kb-init:
        condition: service_completed_successfully
    environment:
      # Legacy runtime value inherited from docker-compose.yml:
      # MONGO_URI: mongodb://mongodb:27017/LibreChat
      MONGO_URI: ""
      # Legacy runtime value inherited from docker-compose.yml:
      # RAG_API_URL: http://rag_api:${RAG_PORT:-8000}
      # Blank so document text extraction uses the native parser instead of rag_api's /text.
      RAG_API_URL: ""
      DYNAMODB_ENDPOINT: ${DYNAMODB_ENDPOINT}
      DYNAMODB_TABLE: ${DYNAMODB_TABLE}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: ${AWS_REGION}
      # DynamoDB's client already falls back to AWS_REGION; this covers callers that only
      # recognize AWS_DEFAULT_REGION (the aws-cli init containers, ministack itself).
      AWS_DEFAULT_REGION: ${AWS_REGION}
      AWS_BUCKET_NAME: librechat-files
      AWS_ENDPOINT_URL: http://ministack:${MINISTACK_PORT}
      AWS_FORCE_PATH_STYLE: "true"
      NO_PROXY: ${NO_PROXY:-localhost,127.0.0.1,::1},${no_proxy:-},meilisearch,chat-meilisearch,rag_api,vectordb,ministack,ollama,host.docker.internal
      no_proxy: ${no_proxy:-localhost,127.0.0.1,::1},${NO_PROXY:-},meilisearch,chat-meilisearch,rag_api,vectordb,ministack,ollama,host.docker.internal
    networks:
      - default
      - infra-network

  ollama:
    image: ollama/ollama:latest
    environment:
      OLLAMA_HOST: 0.0.0.0:${OLLAMA_PORT}
    ports:
      - "${OLLAMA_PORT}:${OLLAMA_PORT}"
    volumes:
      - type: bind
        source: ${OLLAMA_MODELS_PATH}
        target: /root/.ollama/models
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 5s
      timeout: 5s
      retries: 3
    networks:
      - infra-network

  ollama-pull:
    image: ollama/ollama:latest
    environment:
      OLLAMA_HOST: http://ollama:${OLLAMA_PORT}
    command: ["pull", "${OLLAMA_MODEL}"]
    depends_on:
      ollama:
        condition: service_healthy
    networks:
      - infra-network

  ministack:
    image: ministackorg/ministack:latest
    container_name: infra_ministack
    ports:
      - "${MINISTACK_PORT}:${MINISTACK_PORT}"
    depends_on:
      ollama-pull:
        condition: service_completed_successfully
    environment:
      LAMBDA_EXECUTOR: docker
      DOCKER_NETWORK: ${COMPOSE_PROJECT_NAME}_infra-network
      GATEWAY_PORT: ${MINISTACK_PORT}
      AWS_DEFAULT_REGION: ${AWS_REGION}
      MINISTACK_BEDROCK_PROXY_URL: http://ollama:${OLLAMA_PORT}
      MINISTACK_BEDROCK_PROXY_TIMEOUT_SECONDS: 300
      PERSIST_STATE: "1"
      STATE_DIR: /var/lib/ministack/state
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - type: bind
        source: ${MINISTACK_STATE_PATH}
        target: /var/lib/ministack/state
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:${MINISTACK_PORT}/_ministack/health')"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - infra-network

  dynamodb-init:
    image: amazon/aws-cli:2.36.44
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        until aws --endpoint-url "${DYNAMODB_ENDPOINT}" dynamodb list-tables >/dev/null 2>&1; do sleep 2; done
        aws --endpoint-url "${DYNAMODB_ENDPOINT}" dynamodb describe-table --table-name "${DYNAMODB_TABLE}" >/dev/null 2>&1 ||
          aws --endpoint-url "${DYNAMODB_ENDPOINT}" dynamodb create-table \
            --table-name "${DYNAMODB_TABLE}" \
            --billing-mode PAY_PER_REQUEST \
            --attribute-definitions AttributeName=PK,AttributeType=S AttributeName=SK,AttributeType=S AttributeName=GSI1PK,AttributeType=S AttributeName=GSI1SK,AttributeType=S AttributeName=GSI2PK,AttributeType=S AttributeName=GSI2SK,AttributeType=S \
            --key-schema AttributeName=PK,KeyType=HASH AttributeName=SK,KeyType=RANGE \
            --global-secondary-indexes \
              'IndexName=GSI1,KeySchema=[{AttributeName=GSI1PK,KeyType=HASH},{AttributeName=GSI1SK,KeyType=RANGE}],Projection={ProjectionType=ALL}' \
              'IndexName=GSI2,KeySchema=[{AttributeName=GSI2PK,KeyType=HASH},{AttributeName=GSI2SK,KeyType=RANGE}],Projection={ProjectionType=ALL}'
        ttl_status=$$(aws --endpoint-url "${DYNAMODB_ENDPOINT}" dynamodb describe-time-to-live \
          --table-name "${DYNAMODB_TABLE}" \
          --query 'TimeToLiveDescription.TimeToLiveStatus' --output text)
        if [ "$$ttl_status" != "ENABLED" ]; then
          aws --endpoint-url "${DYNAMODB_ENDPOINT}" dynamodb update-time-to-live \
            --table-name "${DYNAMODB_TABLE}" \
            --time-to-live-specification Enabled=true,AttributeName=expiresAt >/dev/null
        fi
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_DEFAULT_REGION: ${AWS_REGION}
    depends_on:
      ministack:
        condition: service_healthy
    restart: "no"
    networks:
      - infra-network

  s3-init:
    image: amazon/aws-cli:2.36.44
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        until aws --endpoint-url "http://ministack:${MINISTACK_PORT}" s3api list-buckets >/dev/null 2>&1; do sleep 2; done
        aws --endpoint-url "http://ministack:${MINISTACK_PORT}" s3api head-bucket --bucket librechat-files >/dev/null 2>&1 ||
          aws --endpoint-url "http://ministack:${MINISTACK_PORT}" s3api create-bucket --bucket librechat-files
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_DEFAULT_REGION: ${AWS_REGION}
    depends_on:
      ministack:
        condition: service_healthy
    restart: "no"
    networks:
      - infra-network

  # Idempotently provisions the shared Bedrock Knowledge Base + S3 data source.
  # ministack's CreateKnowledgeBase returns a server-generated id with no way to pin
  # it from compose, so — like the app's own resolveBedrockKbConfig() — this looks the
  # id up by name first (list-then-create), the same pattern dynamodb-init uses for
  # the table.
  bedrock-kb-init:
    image: amazon/aws-cli:2.36.44
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        until aws --endpoint-url "http://ministack:${MINISTACK_PORT}" bedrock-agent list-knowledge-bases >/dev/null 2>&1; do sleep 2; done
        kb_id=$(aws --endpoint-url "http://ministack:${MINISTACK_PORT}" bedrock-agent list-knowledge-bases \
          --query "knowledgeBaseSummaries[?name=='${BEDROCK_KB_NAME}'].knowledgeBaseId | [0]" --output text)
        if [ "$$kb_id" = "None" ] || [ -z "$$kb_id" ]; then
          kb_id=$(aws --endpoint-url "http://ministack:${MINISTACK_PORT}" bedrock-agent create-knowledge-base \
            --name "${BEDROCK_KB_NAME}" \
            --role-arn "arn:aws:iam::000000000000:role/ministack-kb-role" \
            --knowledge-base-configuration '{"type":"MANAGED","managedKnowledgeBaseConfiguration":{"embeddingModelType":"MANAGED"}}' \
            --query 'knowledgeBase.knowledgeBaseId' --output text)
        fi
        ds_id=$(aws --endpoint-url "http://ministack:${MINISTACK_PORT}" bedrock-agent list-data-sources \
          --knowledge-base-id "$$kb_id" \
          --query "dataSourceSummaries[?name=='${BEDROCK_KB_DATA_SOURCE_NAME}'].dataSourceId | [0]" --output text)
        if [ "$$ds_id" = "None" ] || [ -z "$$ds_id" ]; then
          aws --endpoint-url "http://ministack:${MINISTACK_PORT}" bedrock-agent create-data-source \
            --knowledge-base-id "$$kb_id" \
            --name "${BEDROCK_KB_DATA_SOURCE_NAME}" \
            --data-source-configuration "{\"type\":\"S3\",\"s3Configuration\":{\"bucketArn\":\"arn:aws:s3:::librechat-files\",\"inclusionPrefixes\":[\"${BEDROCK_KB_DATA_SOURCE_PREFIX}\"]}}" \
            >/dev/null
        fi
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_DEFAULT_REGION: ${AWS_REGION}
    depends_on:
      ministack:
        condition: service_healthy
      s3-init:
        condition: service_completed_successfully
    restart: "no"
    networks:
      - infra-network

  mongodb:
    # Legacy service remains declared in docker-compose.yml but is inactive by default.
    profiles:
      - legacy-mongodb

  rag_api:
    # Legacy service remains declared in docker-compose.yml but is inactive by default,
    # superseded by the Bedrock Knowledge Base (bedrock-kb-init + ministack's bedrock-agent).
    profiles:
      - legacy-rag

  vectordb:
    # Legacy service remains declared in docker-compose.yml but is inactive by default.
    profiles:
      - legacy-rag

networks:
  infra-network:

# ---------------------------------------------------

# services:

# # USE LIBRECHAT CONFIG FILE
#   api:
#     volumes:
#     - type: bind
#       source: ./librechat.yaml
#       target: /app/librechat.yaml

# # LOCAL BUILD
#   api:
#     image: librechat
#     build:
#       context: .
#       target: node

# # BUILD FROM LATEST IMAGE
#   api:
#     image: registry.librechat.ai/danny-avila/librechat-dev:latest

# # BUILD FROM LATEST IMAGE (NUMBERED RELEASE)
#   api:
#     image: registry.librechat.ai/danny-avila/librechat:latest

# # BUILD FROM LATEST API IMAGE
#   api:
#     image: registry.librechat.ai/danny-avila/librechat-dev-api:latest

# # BUILD FROM LATEST API IMAGE (NUMBERED RELEASE)
#   api:
#     image: registry.librechat.ai/danny-avila/librechat-api:latest

# # ADD SAML CERT FILE
#   api:
#     volumes:
#     - type: bind
#       source: ./your_cert.pem
#       target: /app/your_cert.pem

# # ADD MONGO-EXPRESS
#   mongo-express:
#     image: mongo-express
#     container_name: mongo-express
#     environment:
#       ME_CONFIG_MONGODB_SERVER: mongodb
#       ME_CONFIG_BASICAUTH_USERNAME: admin
#       ME_CONFIG_BASICAUTH_PASSWORD: password
#     ports:
#       - '8081:8081'
#     depends_on:
#       - mongodb
#     restart: always

# # USE MONGODB V4.4.18 - FOR OLDER CPU WITHOUT AVX SUPPORT
#   mongodb:
#     image: mongo:4.4.18

# # DISABLE THE MONGODB CONTAINER - YOU NEED TO SET AN ALTERNATIVE MONGODB URI IN THE .ENV FILE
#   api:
#     environment:
#       - MONGO_URI=${MONGO_URI}
#   mongodb:
#     image: tianon/true
#     command: ""
#     entrypoint: ""

# # EXPOSE MONGODB PORTS - USE CAREFULLY, THIS MAKES YOUR DATABASE VULNERABLE TO ATTACKS
#   mongodb:
#     ports:
#       - 27018:27017

# # DISABLE MEILISEARCH
#   meilisearch:
#     profiles:
#       - donotstart

# # EXPOSE MEILISEARCH PORTS - DO NOT USE THE DEFAULT VALUE FOR THE MASTER KEY IF YOU DO THIS
#   meilisearch:
#     ports:
#       - 7700:7700

# # USE RAG API IMAGE WITH LOCAL EMBEDDINGS SUPPORT
#  rag_api:
#    image: registry.librechat.ai/danny-avila/librechat-rag-api-dev:latest
# # For Linux user:
#    extra_hosts:
#      - "host.docker.internal:host-gateway"

# # ADD OLLAMA
#  ollama:
#    image: ollama/ollama:latest
#    deploy:
#      resources:
#        reservations:
#          devices:
#            - driver: nvidia
#              capabilities: [compute, utility]
#    ports:
#      - "11434:11434"
#    volumes:
#      - ./ollama:/root/.ollama

# # ADD LITELLM BASIC - NEED TO CONFIGURE litellm-config.yaml, ONLY NEED ENV TO ENABLE REDIS FOR CACHING OR LANGFUSE FOR MONITORING
#  litellm:
#    image: ghcr.io/berriai/litellm:main-latest
#    volumes:
#      - ./litellm/litellm-config.yaml:/app/config.yaml
#      - ./litellm/application_default_credentials.json:/app/application_default_credentials.json # only if using Google Vertex
#    ports:
#      - "4000:8000"
#    command: [ "--config", "/app/config.yaml", "--port", "8000", "--num_workers", "8" ]
#    environment:
#      OPENAI_API_KEY: none ## needs to be set if ollama's openai api compatibility is used
#      GOOGLE_APPLICATION_CREDENTIALS: /app/application_default_credentials.json ## only if using Google Vertex
#      REDIS_HOST: redis
#      REDIS_PORT: 6379
#      REDIS_PASSWORD: RedisChangeMe
#      LANGFUSE_PUBLIC_KEY: pk-lf-RandomStringFromLangfuseWebInterface
#      LANGFUSE_SECRET_KEY: sk-lf-RandomStringFromLangfuseWebInterface
#      LANGFUSE_HOST: http://langfuse-server:3000

# # ADD LITELLM CACHING
#  redis:
#    image: redis:7-alpine
#    command:
#    - sh
#    - -c # this is to evaluate the $REDIS_PASSWORD from the env
#    - redis-server --appendonly yes --requirepass $$REDIS_PASSWORD ## $$ because of docker-compose
#    environment:
#      REDIS_PASSWORD: RedisChangeMe
#    volumes:
#    - ./redis:/data

# # ADD LITELLM MONITORING
#  langfuse-server:
#    image: ghcr.io/langfuse/langfuse:latest
#    depends_on:
#      - db
#    ports:
#      - "3000:3000"
#    environment:
#      - NODE_ENV=production
#      - DATABASE_URL=postgresql://postgres:PostgresChangeMe@db:5432/postgres
#      - NEXTAUTH_SECRET=ChangeMe
#      - SALT=ChangeMe
#      - NEXTAUTH_URL=http://localhost:3000
#      - TELEMETRY_ENABLED=${TELEMETRY_ENABLED:-true}
#      - NEXT_PUBLIC_SIGN_UP_DISABLED=${NEXT_PUBLIC_SIGN_UP_DISABLED:-false}
#      - LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES=${LANGFUSE_ENABLE_EXPERIMENTAL_FEATURES:-false}
#  db:
#    image: postgres
#    restart: always
#    environment:
#      - POSTGRES_USER=postgres
#      - POSTGRES_PASSWORD=PostgresChangeMe
#      - POSTGRES_DB=postgres
#    volumes:
#      - ./postgres:/var/lib/postgresql/data
